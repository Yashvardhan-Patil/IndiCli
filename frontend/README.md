# IndiCli Frontend

React + Vite frontend for IndiCli.

## Development

```bash
npm install
npm run dev
```

Frontend runs at `http://localhost:5173` and calls the FastAPI backend configured by `VITE_API_BASE_URL`, usually `http://localhost:8000`.
